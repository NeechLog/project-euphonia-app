# ============================================
# OAuth2.0 / OpenID Connect (OIDC) Configuration
# ============================================
# Copy this file to .env.oidc and update the values accordingly
# Keep this file secure and never commit sensitive data to version control

# ================
# Common Settings
# ================
# Base secret key used as fallback if provider-specific keys are not set
STATE_SECRET_KEY=change-me-to-a-secure-random-string

# ================
# Generic OIDC (for stateless.py)
# ================
# OIDC Token Endpoint (used for direct OIDC integration)
OIDC_TOKEN_ENDPOINT=https://your-oidc-provider.com/oauth/token
# OIDC Client ID for generic OIDC provider
OIDC_CLIENT_ID=your-oidc-client-id
# OIDC Client Secret for generic OIDC provider
OIDC_CLIENT_SECRET=your-oidc-client-secret
# OIDC Issuer URL (used for JWT validation)
OIDC_ISSUER=https://your-oidc-provider.com/
# OIDC Audience (aud claim) for JWT validation
OIDC_AUDIENCE=your-audience
# URL to fetch JSON Web Key Set (JWKS) for JWT validation
OIDC_JWKS_URL=https://your-oidc-provider.com/.well-known/jwks.json

# ================
# Google OAuth
# ================
# Google OAuth Client ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# Google OAuth Client Secret
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Google OAuth Token Endpoint (usually doesn't need to be changed)
GOOGLE_TOKEN_ENDPOINT=https://oauth2.googleapis.com/token
# Optional: Override the default state secret key for Google
# GOOGLE_STATE_SECRET_KEY=change-me-google-secret

# ================
# Apple Sign In
# ================
# Apple Services ID (Client ID)
APPLE_CLIENT_ID=com.your.app.bundle.id
# Apple Team ID (find in Apple Developer Account)
APPLE_TEAM_ID=YOUR_TEAM_ID
# Apple Key ID (from the key you created in Apple Developer Account)
APPLE_KEY_ID=YOUR_KEY_ID
# Path to your Apple Auth Key .p8 file
APPLE_AUTH_KEY_PATH=/path/to/AuthKey_XXXXXXXXXX.p8
# Apple Token Endpoint (usually doesn't need to be changed)
APPLE_TOKEN_ENDPOINT=https://appleid.apple.com/auth/token
# Optional: Override the default state secret key for Apple
# APPLE_STATE_SECRET_KEY=change-me-apple-secret

# ================
# Session Configuration
# ================
# Secret key for session encryption
SESSION_SECRET_KEY=change-me-to-a-secure-random-session-key
# Session cookie name
SESSION_COOKIE_NAME=session_cookie
# Session lifetime in seconds (default: 7 days)
SESSION_LIFETIME=604800

# ================
# Security Settings
# ================
# Comma-separated list of allowed origins for CORS
# Example: http://localhost:3000,https://your-production-domain.com
ALLOWED_ORIGINS=*
# Enable/disable HTTPS only cookies (recommended: True in production)
SECURE_COOKIES=True
# Enable/disable HTTP only flag for cookies (recommended: True)
HTTP_ONLY_COOKIES=True
# Enable/disable SameSite attribute for cookies (recommended: 'lax' or 'strict')
SAME_SITE_COOKIES=lax
